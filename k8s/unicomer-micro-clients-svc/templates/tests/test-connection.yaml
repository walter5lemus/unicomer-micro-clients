apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unicomer-micro-clients-svc.fullname" . }}-test-connection"
  labels:
    {{- include "unicomer-micro-clients-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unicomer-micro-clients-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
