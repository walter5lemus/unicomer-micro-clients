apiVersion: v1
kind: Service
metadata:
  name: {{ include "unicomer-micro-clients-svc.fullname" . }}
  labels:
    {{- include "unicomer-micro-clients-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unicomer-micro-clients-svc.selectorLabels" . | nindent 4 }}
